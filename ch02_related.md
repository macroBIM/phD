# 제2장 관련연구

## 2.1 상용 BIM/클래시탐지 도구

- 대상 도구와 각 도구의 간섭 처리 지점(모델링 시점 / 검토 시점)을 표로 정리한다.
- 공통 전제: 간섭은 **모델이 완성된 뒤** 규칙 기반으로 검출된다.
- 이 전제가 본 연구의 대비축(제8장 H5)이 된다.

> 작성 예정: 도구별 비교표(입력 형식, 간섭 판정 기준, 수정 자동화 수준, 출처).

## 2.2 APF/최적화 기반 rebar layout 연구

- 인공 포텐셜장(APF)의 표준형:

$$U(\mathbf{x}) = U_{\text{att}}(\mathbf{x}) + \sum_{i=1}^{n} U_{\text{rep},i}(\mathbf{x})$$

- 경로계획에서 넘어온 이 정식화가 배근 문제에 적용될 때 무엇이 달라지는지(목표점이 아닌
  *목표 경계*, 이동체가 아닌 *동시 배치*)를 정리한다.
- 최적화 기반(유전 알고리즘·혼합정수계획) 배근 연구의 목적함수와 제약 처리 방식을 비교한다.

> 작성 예정: 문헌별 표 — 대상 부재, 장애물 모델, 해법, 검증 규모.

## 2.3 파라메트릭/규칙기반 생성설계

- 파라메트릭 배근(규칙 스크립트) 계열이 다루는 범위와, 비정형 단면에서 규칙이 깨지는 지점.
- 본 연구와의 관계: 규칙기반은 *형상별 규칙*을, CAF는 *형상 독립적 장(field)*을 쓴다.

> 작성 예정: 선행연구 대비 본 연구의 위치를 나타내는 좌표(2×2) 그림.
