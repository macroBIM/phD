# 제2장 관련연구

## 2.1 상용 BIM/클래시탐지 도구

- **구조해석 계열**(Robot, ETABS): 소요 철근량을 산출하지만 철근의 공간 좌표를 생성하지 않는다.
- **클래시 탐지 계열**(Navisworks, Solibri): 완성된 모델을 입력으로 받아 간섭을 판정한다.
  회피는 사람이 모델을 고쳐서 한다 — 탐지와 수정이 분리된 detect-and-fix 워크플로우.
- **4D 시뮬레이션**: 시공 순서를 다루지만 단위가 부재이며, 철근 개별 단위의 간섭은 대상이 아니다.

이 계열들의 공통 전제는 *간섭은 모델이 만들어진 뒤에 발견된다*는 것이다. 이 전제가 제8장의
비교축(H5)이 된다.

## 2.2 APF/최적화 기반 rebar layout 연구

인공 포텐셜장의 표준형은 인력항과 척력항의 합이다.

$$U(\mathbf{x}) = U_{\text{attr}}(\mathbf{x}) + U_{\text{rep}}(\mathbf{x})$$

기존 연구(Liu 2020, Xu 2022)에서 인력항의 목표는 **외부에서 주어진 0차원 상수점** $\mathbf{x}_0$ 다.
따라서 이들이 푸는 문제는 "주어진 시작점에서 주어진 목표점까지 어떻게 가는가"(routing)이며,
"목표점 자체를 단면에서 어떻게 뽑는가"(placement)는 문제의 밖에 있다. 배근에서는 후자가 본질이다.

최적화·AI 기반 연구 역시 목적함수로 철근 물량·개수를 쓰는 경우가 많아, 기하 간섭은 제약의
일부로 부차적으로 처리된다.

## 2.3 파라메트릭/규칙기반 생성설계

Dynamo 등 파라메트릭 도구는 절차적으로 좌표를 계산한다. 한계는 위상적 특수 사례에서 드러난다.

- **실증된 반례**: I빔 복부의 오목 코너(reflex corner)에서 단순 polygon offset 은
  self-intersection 을 일으킨다.
- robust offset(예: Shapely `buffer`)은 이 자기교차를 해소한다. 그러나 코너 유형별로
  topology-aware 로직을 사람이 마련해야 한다는 한계는 그대로 남는다.
- **이 지점이 CAF 와 파라메트릭 방법을 가르는 핵심 비교점**이다. CAF 는 코너 유형을 분기하지 않고
  장(field) 하나로 처리한다.

> 3패널 반례 그림(단순 offset 자기교차 / robust offset / CAF)을 이 절의 대표 그림으로 쓴다.
