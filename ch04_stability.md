# 제4장 수렴성 및 안정성

## 4.1 H2'-a — 수치안정성 (파라미터 안정조건 유도)

**H2'-a (주장).** 이완 스텝 $\Delta t$, 인력계수 $k_a$, 척력계수 $k_r$ 사이에
수렴을 보장하는 부등식이 존재한다.

$$\Delta t < \frac{2}{L}, \qquad L = \sup_{\mathbf{x}\in\Omega} \lVert \nabla^2 U_{\text{CAF}}(\mathbf{x}) \rVert$$

- 유도 전략: 기울기 흐름의 Lipschitz 상수 상한 → 스텝 제한 → 이산 반복의 단조감소.
- 실무적 산출물: 피복·간격·덕트 반경으로부터 $\Delta t$ 를 정하는 표.

> 작성 예정: $L$ 의 상한을 장애물 최소이격거리로 표현하는 보조정리.

## 4.2 H2'-b — 기하학적 도달가능성 (장애물 배치 실패모드)

**H2'-b (주장).** 장애물 배치에 따라 도달 불가능한 영역이 생기며, 그 조건은 기하학적으로
판정할 수 있다.

실패모드 분류(작성 중):

1. **폐색(occlusion)** — 장애물이 경계와 만나 통로를 끊는 경우.
2. **국소최소(local minimum)** — 인력과 척력이 상쇄되어 정지하는 경우.
3. **협착(narrow passage)** — 통로 폭이 철근 지름 + 최소간격보다 좁은 경우.

> 작성 예정: 각 모드의 판정식과, 판정 실패 시 사용자에게 돌려줄 진단 메시지 설계.

## 4.3 H2'-c — 군집 장애물 채터링 문제와 해법 (그룹 무게중심 반발장)

**H2'-c (주장).** 장애물이 군집(덕트 클러스터)을 이루면 개별 척력의 합이 진동(채터링)을
일으키며, 군집을 하나의 무게중심 반발장으로 대체하면 이를 제거할 수 있다.

$$\mathbf{c}_G = \frac{\sum_{o\in G} m_o \mathbf{x}_o}{\sum_{o\in G} m_o}$$

- 군집 판정(클러스터링) 기준, 군집 반경 $R_G$ 의 결정, 개별장 ↔ 군집장 전환 임계.
- 채터링을 정량화할 지표(변위 진폭·반복수) 정의.

> 작성 예정: 전환 조건의 유도와 채터링 지표의 실험적 확인.

## 4.4 파라미터 결합 민감도 분석

- 대상 파라미터: $k_a$, $k_r$, $\rho_0$, $\Delta t$, 군집 임계.
- 방법: 1인자 스윕 + 2인자 격자, 수렴 반복수와 최종 간섭 개수를 응답으로 본다.

> 작성 예정: 민감도 히트맵과 권장 파라미터 구간.
