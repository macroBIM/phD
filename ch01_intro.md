# 제1장 서론

## 1.1 연구 배경

철근콘크리트 배근은 여전히 수작업 또는 반자동에 머물러 있다. 문제는 한 곳이 아니라 여러 층위에
걸쳐 있다.

1. **구조해석 소프트웨어**(Robot, ETABS 등)는 철근 *물량*을 산출할 뿐 공간상의 배치를 하지 않는다.
2. **클래시 탐지 도구**(Navisworks, Solibri)는 이미 만들어진 모델에서 간섭을 *탐지*할 뿐
   회피 로직을 갖지 않는다 — detect-and-fix.
3. **4D 시뮬레이션**은 부재 단위로 동작하며 철근 개별 단위가 아니다.
4. 직교부재 간섭, 타공종(덕트·배수관) 간섭, 시공여유(폼타이 등)가 설계 단계에서 누락된다.
5. 기존 최적화·AI 연구도 철근 *물량/개수* 를 대리지표로 삼을 뿐, 실제 기하 간섭은 부차적으로 다룬다.

기존 인공 포텐셜장(APF) 연구(Liu 2020, Xu 2022)는 목표점을 **외부에서 미리 지정한 0차원 상수점**
으로 둔다. 그 결과 좌표와 좌표 사이의 *이동*(routing)은 풀지만, 좌표 자체를 *생성*(placement)
하지는 못한다. 파라메트릭·규칙기반 접근(Dynamo 등)은 절차적으로 좌표를 계산하지만, 오목 코너
같은 위상적 특수 사례마다 사람이 규칙을 새로 짜야 한다.

## 1.2 연구 필요성 및 기여

**Gap.**

- 단면 형상만으로 목표 위치를 자동·생성적으로 도출하는 이론적 틀이 없다.
- 텐던 덕트·개구부 등 복합 경계를 통합적으로 다루는 방법론이 부족하다.
- 군집 장애물(clustered obstacles)에서의 국소 안정성(채터링) 문제는 기존 문헌에 미해결로 남아 있다.
- avoidance-by-construction 접근의 이론적·정량적 근거가 없다.

**기여.** 이 논문의 무게중심은 아래 가설들의 *실제 증명*이며, 사례연구는 기여가 아니라
증명의 검증 수단이다.

| 가설 | 기여 | 대응 |
|---|---|---|
| H1 | Cover-Attractive Field 의 well-definedness — attractor 판별을 방향정합성(내적) 조건으로 명시 | 3.2 |
| H2' | 장애물 존재 하의 수렴성 — 수치안정성 / 도달가능성 / 군집 채터링의 3단 구조 | 4.1–4.3 |
| H3 | 기존 APF 는 CAF 의 0차원 퇴화 특수 사례 | 3.3 |
| H4 | 비정형 단면(환형·비볼록·곡선경계) 일반화 | 6.1 |
| H5 | avoidance-by-construction 대 detect-and-fix 의 정량 비교 | 8.1 |
| H6 | 다중경계 CAF — 덕트·개구부·기배근·거푸집을 경계로 편입하고 LSAP 로 정식화 | 5.1 |

## 1.3 연구 범위

- **스코프**: Path 3-Lightweight — 이론 챕터(CAF 정식화) + 구현 문서 + 3개 사례연구 검증.
- **제외**: 저널 출판, 사용자 연구.
- **유보**: 3D 확장은 명시적으로 향후 연구로 미루고 **2D 단면에 한정**한다.
  RAF(Rebar-Attractive Field) 역시 확장 가능성으로만 언급하고 증명·구현은 하지 않는다(4.3 참조).
- **파라미터 원칙**: $\alpha$, `GRAVITY_K`, `DAMPING`, `CONVERGE` 등을 하드코딩된 매직넘버로
  남기지 않고 이론적·실증적으로 정당화한다.
- **구현 대상 저장소**: `macroBIM/macroBIM` (물리 시뮬레이션 엔진 — `physics.js`,
  `domain.js`, `lrebar.js`, `trebar.js`), `macroBIM/design` (단면·철근 데이터베이스, 정착길이).
  주의: `mod_rebar_leng.js` 의 `alpha1`~`alpha6` 은 설계기준 정착길이 계수로, CAF 의 $\alpha$ 와
  무관한 별개 개념이다.

## 1.4 논문 구성

제2장에서 상용 도구·APF·파라메트릭 연구를 검토해 gap 을 확정한다. 제3장에서 포텐셜을 정식화하고
H1·H3 을 다루며, 제4장에서 수렴성을 세 단계로 나누어 증명한다. 제5장은 다중경계 확장,
제6장은 비정형 단면 일반화, 제7장은 세 개의 사례연구, 제8장은 패러다임 비교, 제9장은 결론이다.
