# 제5장 다중경계 Cover-Attractive Field

## 5.1 H6 — 다중경계 이산할당 정식화

**H6.** 덕트·개구부·기배근·거푸집을 추가 경계로 편입해 유효 attractor 를 경계들의 합집합으로 둔다.

$$\Gamma_{\text{eff}} = \Gamma_{\text{outer}} \cup \Gamma_{\text{duct},1} \cup \Gamma_{\text{duct},2} \cup \cdots$$

이때 각 세그먼트를 어느 경계 조각에 배정할지가 문제가 되며, 이를 **선형합 할당 문제(LSAP,
Linear Sum Assignment Problem)** 또는 **최소비용흐름(Min-Cost Flow)** 으로 정식화한다.

$$\min_{X} \sum_{i}\sum_{j} C_{ij}\, x_{ij}, \qquad
\sum_{j} x_{ij} = 1,\quad x_{ij} \in \{0,1\}$$

비용행렬 $C_{ij}$ 는 세그먼트 $i$ 를 경계 조각 $j$ 에 배치할 때의 **거리 비용과 방향 비용**의
합으로 둔다. 방향 비용은 3.2 의 방향정합성 조건과 같은 내적을 쓴다.

## 5.2 그룹 반발장과의 결합

- 4.3 의 자동 그룹핑은 척력만의 문제가 아니다. 그룹핑 결과가 LSAP 할당의 안정성에도 영향을 준다 —
  군집이 경계 조각 여러 개에 걸치면 할당이 스텝마다 흔들릴 수 있다.
- 따라서 5장의 이론과 4.3 의 해법은 서로 보강하는 구조로 서술한다. H2'(특히 채터링)가
  H6 의 필요성을 보여주는 동기(motivation) 역할도 겸한다.

> 작성 예정: 그룹핑 → 할당 → 이완의 순서를 확정한 결합 알고리즘 의사코드.
