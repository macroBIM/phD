# 제5장 다중경계 Cover-Attractive Field

## 5.1 H6 — 다중경계 이산할당 정식화

**H6.** 덕트·개구부·기배근·거푸집을 추가 경계로 편입한다. 정의 3.1의 피복선을 이들까지 포함해
확장하는 것으로 충분하다.

$$\Gamma_{\text{cover}} = \Gamma_{\text{outer}} \cup \Gamma_{\text{duct},1} \cup \Gamma_{\text{duct},2} \cup \cdots$$

각 덕트·개구부도 자기 경계와 자기 피복을 가지므로 정의 3.1이 그대로 적용된다.
그러면 정의 3.2b 의 확장 필터 $A(\mathbf{n}, \sigma)$ 가 **방향과 역할로** 후보를 좁히고, 광선
투사가 그중 하나를 고른다. 역할 라벨이 없으면 이 절의 "경계로 편입한다"가 어느 철근에 대한
경계인지 정해지지 않는다.

문제는 **광선 투사가 세그먼트 하나씩 독립으로 고른다**는 데 있다. 세그먼트 여럿이 같은 조각에
몰리거나, 인접 세그먼트가 서로 다른 경계에 붙어 배근이 갈라질 수 있다. 그래서 배정을 전역
문제로 다시 세운다 — **선형합 할당 문제(LSAP, Linear Sum Assignment Problem)** 또는
**최소비용흐름(Min-Cost Flow)**.

$$\min_{X} \sum_{i}\sum_{j} C_{ij}\, x_{ij}, \qquad
\sum_{j} x_{ij} = 1,\quad x_{ij} \in \{0,1\}$$

비용행렬 $C_{ij}$ 는 세그먼트 $i$ 를 경계 조각 $j$ 에 배치할 때의 **거리 비용과 방향 비용**의
합으로 둔다. 방향 비용은 3.2 의 방향정합성 조건과 같은 내적을 쓴다.

## 5.2 그룹 반발장과의 결합

- 4.3 의 자동 그룹핑은 척력만의 문제가 아니다. 그룹핑 결과가 LSAP 할당의 안정성에도 영향을 준다 —
  군집이 경계 조각 여러 개에 걸치면 할당이 스텝마다 흔들릴 수 있다.
- 따라서 5장의 이론과 4.3 의 해법은 서로 보강하는 구조로 서술한다. H2'(특히 채터링)가
  H6 의 필요성을 보여주는 동기(motivation) 역할도 겸한다.

> 작성 예정: 그룹핑 → 할당 → 이완의 순서를 확정한 결합 알고리즘 의사코드.

## 5.3 적층 배치의 이산 최적화

> **새로 추가한 절이다.** 목차에 없던 항목이므로, 유지 여부는 별도로 판단해야 한다.

### 문제 — 현재는 순서가 답을 정한다

구현은 철근을 입력 큐 순서대로 하나씩 안착시키고, 안착할 때마다 그 벽 구간에 두께를 누적한다
(`domain.js:29, 252, 340`). 다음 철근은 그 적층만큼 앞에서 멈춘다(3.4의 $\delta = \phi/2 + s$).

$$\text{큐 순서} \;\longrightarrow\; \text{층 배정} \;\longrightarrow\; \text{유효깊이}$$

**순차적 그리디**이며 목적함수가 없다. 순서를 바꾸면 다른 답이 나오고, 어느 답이 나은지 판단할
기준이 이론에도 코드에도 없다. 3.1절의 역학적 근거가 그 기준을 준다.

### 정식화

층 $\ell = 1, 2, \dots$ 을 인장연단에서부터 세고, 철근 $j$ 를 어느 층에 둘지를 결정변수로 둔다.

$$\max_{x} \; \sum_{j}\sum_{\ell} A_{j}\, d_{\ell}(x)\, x_{j\ell}
\qquad\text{s.t.}\quad
\sum_{\ell} x_{j\ell} = 1,\quad
\sum_{j} x_{j\ell} \le n_{\ell},\quad
x_{j\ell} \in \{0,1\}$$

- $A_j$ : 철근 $j$ 의 공칭 단면적
- $n_\ell$ : 층 $\ell$ 의 수용 개수 — 벽 길이와 최소 순간격에서 정해진다
- $d_\ell(x)$ : 층 $\ell$ 의 유효깊이

**5.1의 LSAP 과 결정적으로 다른 점이 하나 있다.** $d_\ell$ 이 상수가 아니라 **해에 의존한다** —
층 $\ell$ 의 위치는 그보다 바깥 층에 놓인 철근들의 지름 합으로 정해지기 때문이다.

$$d_\ell(x) = h - \Big( c_w + \sum_{m<\ell}\big[\phi_{\max}(m) + s_{\text{clear}}\big] + \frac{\phi_{\max}(\ell)}{2} \Big)$$

따라서 비용행렬이 고정된 선형합 할당이 아니라 **결합된(coupled) 할당 문제**다. 5.1의 해법을
그대로 쓸 수 없으며, 층 구성이 정해지면 비용이 정해지는 구조를 이용한 반복 해법
(층 구성 고정 → LSAP → 갱신)이 후보다.

> 작성 예정: 이 반복이 수렴하는지, 최적성 간극의 상한이 있는지.

### 명제 5.1 (그리디는 순서에 의존하며 최적이 아니다)

같은 철근 집합이라도 큐 순서에 따라 목적함수 값이 달라진다. 반례를 제원으로 확인한다
(`macroBIM/design/rebar_ks.csv`, $h=1000$, $c_w=40$, $s_{\text{clear}}=25$ mm,
층 용량 2개, 철근 D32 2개 + D16 2개).

| 배치 | 층1 $d$ | 층2 $d$ | $\sum A_j d_j$ | 도심 $\bar{d}$ |
|---|---|---|---|---|
| 그리디 (큐 순서상 D16 이 먼저) | 952.0 | 903.2 | 1,812,797 | 913.0 |
| 최적 (큰 지름을 바깥층에) | 944.1 | 895.2 | **1,855,202** | **934.3** |

**그리디는 최적 대비 $\sum A_j d_j$ 가 2.29 % 작다.** 같은 강재량으로 모멘트 팔을 그만큼
손해 본다는 뜻이며, 3.1의 등가에 의해 $M_n$ 이 그만큼 작아진다.

주목할 점은 **층별 유효깊이만 보면 그리디가 더 크다**는 것이다(952.0 > 944.1). 얇은 D16 이
바깥층을 차지해 층1 자체는 연단에 가까워지지만, 면적이 큰 D32 가 안쪽으로 밀려 가중합에서
손해를 본다. 층 위치만 보고 판단할 수 없고 **면적 가중이 필요하다**는 것이 이 반례의 요지다.

### 이 절이 논문에서 하는 일

1. **역학이 들어온다** — 목적함수가 휨저항이며, 기하 규칙이 아니다.
2. **최적화가 들어온다** — 5.1과 같은 이산 할당 계열이라 이론이 따로 놀지 않는다.
3. **현재 구현이 근사해로 자리 잡는다** — 그리디와 최적의 간극을 사례별로 재면 정량 결과가 된다.
