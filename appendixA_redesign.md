# 부록 A. 연구 재설계 검토 (제안)

> 이 문서는 논문 본문이 아니라 **연구 방향을 다시 세우기 위한 검토서**다.
> 제3–5장의 현재 정식화를 대체할 것을 제안하며, 채택 여부는 별도 판단이 필요하다.

## A.1 왜 다시 세우는가

현재 정식화(3.1)를 코드대로 적고 나니 한 가지가 분명해졌다.

$$T = \mathbf{x} + \Big(\tau - \frac{\delta}{\cos}\Big)\mathbf{d}
\quad\text{를 먼저 계산하고,}\quad
U = \tfrac12 k\,r^2 \text{ 를 46스텝 이완해 그 } T \text{ 에 도달한다.}$$

**이미 닫힌형으로 구한 점에 반복법으로 수렴해 간다.** 장(field)이 하는 일이 없다.
"왜 포텐셜이 필요한가"에 답할 수 없고, H3(APF 일반화)는 정의상 자명함에 머문다.

장이 실제로 일하려면 **경쟁하는 요구가 동시에 있어야** 한다. 그런데 그 요구를 만드는 것들 —
덕트, 개구부, 다른 철근, 역학적 우선순위 — 이 현재 스코프 밖이거나 미구현이다.
방법론을 정당화하는 부분이 통째로 빠져 있는 셈이다.

**따라서 문제를 바꾼다.** "한 철근이 목표로 이동하는 문제"에서
**"단면 전체에 강재를 어떻게 분포시킬 것인가"** 로 올린다. 후자에서는 장이 목적 그 자체다.

## A.2 재정식화 — 배치를 "수요장의 이산 근사"로

### A.2.1 영역과 허용집합 (기하)

단면 영역 $\Omega \subset \mathbb{R}^2$ 의 경계를 종류별로 나눈다.

$$\partial\Omega = \Gamma_{\text{out}} \cup \Gamma_{\text{cell}} \cup \Gamma_{\text{duct}} \cup \Gamma_{\text{open}}$$

각 경계에 고유 피복 $c(\gamma)$ 를 주고, **부호거리장**으로 허용집합을 정의한다.

$$\Phi(\mathbf{x}) = \min_{\gamma \in \partial\Omega} \frac{\operatorname{dist}(\mathbf{x}, \gamma)}{c(\gamma)},
\qquad
\mathcal{A}(\phi) = \Big\{\mathbf{x}\in\Omega \;:\; \Phi(\mathbf{x}) \ge 1 + \tfrac{\phi}{2c}\Big\}$$

**여기서 덕트와 개구부는 특별 취급이 없다.** 경계 목록에 한 줄 더 들어갈 뿐이며,
중공·다중셀 같은 위상도 부호거리장이 그대로 처리한다. 회피가 알고리즘이 아니라
**정의역의 성질**이 된다 — avoidance-by-construction 의 문자 그대로의 구현이다.

### A.2.2 수요장 (역학)

$\rho(\mathbf{x}) \ge 0$ 을 **위치 $\mathbf{x}$ 의 단위 강재면적이 갖는 역학적 가치**로 둔다.
휨지배 단면에서 중립축 깊이를 $c_{na}$ 라 하면, 변형률 적합에서

$$\rho(\mathbf{x}) \;=\; f_s\big(\varepsilon(\mathbf{x}; c_{na})\big)\cdot z(\mathbf{x}; c_{na})$$

($f_s$ 는 철근 응력, $z$ 는 팔길이.) 전단·비틀림 지배에서는 수요가 2차원 밀도가 아니라
**둘레를 따르는 1차원 측도**가 된다. 두 경우를 하나로 다루기 위해 수요를 밀도가 아니라
**측도** $\mu$ 로 둔다 — 2D 밀도든 곡선 위 선측도든 같은 틀에서 다룬다.

### A.2.3 배치 = 측도 근사

철근 배치를 이산 측도로 본다.

$$\nu = \sum_{i=1}^{N} A_i\,\delta_{\mathbf{x}_i},
\qquad \mathbf{x}_i \in \mathcal{A}(\phi_i)$$

그러면 배근 문제는 **연속 수요 $\mu$ 를 이산 측도 $\nu$ 로 가장 잘 근사하는 문제**다.

$$\min_{\{\mathbf{x}_i\},\{\phi_i\},N} \; \mathcal{W}(\mu, \nu)
\qquad\text{s.t.}\quad
\lVert \mathbf{x}_i - \mathbf{x}_j \rVert \ge s_{\min}(\phi_i,\phi_j),\quad
\sum_i A_i \ge A_{s,\text{req}}$$

$\mathcal{W}$ 는 측도 간 거리(Wasserstein 또는 CVT 에너지)다. 이 형태의 값어치는 세 가지다.

1. **장이 목적 그 자체다.** 더 이상 "이미 아는 점으로 가는 길"이 아니다.
2. **역학이 목적함수에 있다.** $\mu$ 가 역학에서 나온다.
3. **기성 이론이 있다.** 아래 계열의 수렴·최적성 결과를 끌어올 수 있다.

| 계열 | 무엇을 주는가 | 대표 문헌(서지 확인 필요) |
|---|---|---|
| 중심 보로노이 분할(CVT)·Lloyd 반복 | 밀도에 비례하는 점 분포, 에너지 감소 증명 | Du, Faber, Gunzburger (1999) |
| 용량제약 점분포 | 각 점이 담당할 "양"을 강제 | Balzer, Schlömer, Deussen (2009) |
| 최적수송 기반 blue noise | 측도 근사로서의 점 배치 | de Goes 외 (2012) |
| 가변반경 Poisson-disk | 최소간격을 만족하는 표본 | Bridson (2007) |
| 레벨셋·부호거리장 | 임의 위상 영역 표현 | Osher–Sethian 계열 |

## A.3 덕트·개구부는 장에 **두 번** 나타난다

이 재설계의 핵심이며, 질문에 대한 직접적인 답이다.

### 역할 ① 허용영역의 구멍 — 회피

$\Gamma_{\text{duct}}$ 가 경계 목록에 들어가면 $\mathcal{A}$ 에서 덕트 주변이 자동으로 파인다.
철근은 덕트를 "피하는" 것이 아니라 **애초에 그곳에 정의되지 않는다**.
척력장도, 예외 규칙도 필요 없다.

### 역할 ② 수요의 원천 — 보강근

덕트·개구부는 단면을 약화시키고 주변에 응력을 집중시킨다. 따라서 그 둘레에는
**추가 수요**가 생긴다.

$$\mu = \underbrace{\mu_{\text{flex}}}_{\text{휨}}
   \;+\; \underbrace{\mu_{\text{shear}}}_{\text{전단·둘레측도}}
   \;+\; \underbrace{\sum_{k}\mu_{\text{open},k}}_{\text{개구부 보강}}$$

$\mu_{\text{open},k}$ 는 개구부 $k$ 주변의 응력집중에서 유도한다.
**덕트가 한쪽에서는 정의역을 깎고 다른 쪽에서는 수요를 만든다** — 이 이중성이
"장으로 배근한다"는 말이 실제로 성립하는 지점이다. 지금까지의 척력/인력 논쟁
(정의 3.2b의 역할 라벨)이 여기서는 **두 항의 분리**로 자연히 해소된다.

## A.4 해결해야 할 문제

박사논문이 실제로 다뤄야 할 난제 목록이다. 각각 왜 어려운지와 무엇을 증명해야 하는지를 적는다.

### P1. 수요장과 중립축의 결합 (가장 중요)

$\rho$ 는 중립축 $c_{na}$ 에 의존하고, $c_{na}$ 는 배치의 결과다.

$$\rho = F(c_{na}), \qquad \nu = G(\rho), \qquad c_{na} = H(\nu)$$

즉 **고정점 문제** $c_{na} = H(G(F(c_{na})))$ 다. 배치가 중립축을 바꾸고 중립축이 배치의
가치를 바꾼다. 증명할 것: 고정점의 **존재·유일성·반복의 수렴**. 단조성이나 축소사상
성질을 보일 수 있다면 정리가 된다.
**이것이 이 논문의 이론적 중심이 될 후보다** — APF 일반화보다 훨씬 방어하기 쉽고,
구조공학과 최적화가 만나는 자리다.

### P2. 허용집합의 존재성과 진단

$\mathcal{A}$ 가 비거나 끊어지면 배치가 불가능하다. 필요조건(면적·폭 하한)과
**실행 불가능을 사전에 판정하고 원인을 지목하는 진단**을 세워야 한다.
현재 4.2의 실패모드 7종이 여기로 흡수된다 — 이완 중 발견하는 현상이 아니라
**기하에서 미리 판정하는 조건**이 된다.

### P3. 연속 → 이산 (양자화)

연속 수요 $\mu$ 를 표준 지름 집합 $\{D10, D13, \dots\}$ 의 유한개 철근으로 옮기는 문제.
개수 $N$ 과 지름 조합이 결정변수인 혼합정수 문제이며, **근사 오차의 상한**이 필요하다.
현재 5.3의 적층 최적화가 이 문제의 1차원 특수 사례다.

### P4. 최소간격 = 비겹침 제약

하드코어 제약이라 비볼록이다. 벌칙항으로 완화할지, 투영으로 강제할지,
가변반경 Poisson-disk 로 처음부터 만족시킬지 — **선택과 그 정당화**가 필요하다.
완화를 쓴다면 수렴 시점에 제약이 실제로 만족되는지 보여야 한다.

### P5. 수요 측도의 유도

$\mu_{\text{flex}}$, $\mu_{\text{shear}}$, $\mu_{\text{open}}$ 을 각각 어디서 가져올 것인가.
파이버 단면해석에서 유도할지, 설계기준의 요구량에서 역산할지, 탄성해석 결과를 쓸지.
**가정과 적용범위를 명시**해야 하며, 이 부분이 심사에서 가장 많이 질문받을 곳이다.

### P6. 유일성과 초기값 의존성

CVT 류는 국소최소가 다수 존재한다. 서로 다른 초기 배치가 다른 해로 가는지,
간극이 얼마인지 특성화해야 한다. 현재 구현의 **큐 순서 의존성**(5.3)이 이 문제의
원시적 형태다.

### P7. 시공성과 층 구조

같은 층에 놓인다는 것, 스터럽이 주철근을 감싼다는 것, 정착·이음 —
연속 밀도에는 없는 **이산적·위상적 요구**를 어떻게 제약으로 넣을 것인가.

### P8. 검증

- 실도면 대조: 확보한 PSC I빔 실배근을 ground truth 로 좌표·개수·$M_n$ 비교.
- 기준 충족: 피복·최소간격·최대간격 자동 판정.
- 패러다임 비교: detect-and-fix 워크플로 모사 baseline (현재 H5).

## A.5 기존 작업에서 무엇이 이어지는가

버리는 것보다 이어지는 것이 많다.

| 기존 자산 | 재설계에서의 자리 |
|---|---|
| 피복선 생성(`buildCoverWalls`), 코너 재교점 | A.2.1 허용집합 계산의 기하 엔진 — 그대로 |
| 단면 엔진(box1cell·ibeam·rect·circle·octagon·track) | 임의 위상 검증 도구 — 그대로 |
| 방향정합성 $\alpha_{\text{th}}$ | 국소 규칙에서 **경계 귀속의 진단 지표**로 격하 |
| 광선 투사·이완(heavy-ball) | 초기해 생성기 또는 투영 연산자로 재활용 가능 |
| Jury 안정조건, 명제 4.1($1/\cos$ 오차) | 이완을 쓰는 한 유효 — 부록으로 이동 |
| 채터링·그룹 반발장 | P6(유일성·초기값 의존성)의 사례로 흡수 |
| 사례 3종, 실도면 | P8 검증 — 그대로, 오히려 더 중요해짐 |
| 5.3 적층 최적화 | P3(양자화)의 1차원 특수 사례로 승격 |

**"CAF" 라는 이름도 살릴 수 있다.** 피복선을 향한 인력은 $\mu_{\text{flex}}$ 가 인장연단에서
최대가 된다는 사실의 결과로 **유도**되기 때문이다(3.1의 역학적 근거). 즉 CAF 는 가정이
아니라 정리가 된다 — 이것이 H3 을 자명함에서 구해내는 길이기도 하다.

## A.6 최소 실행 계획

한 번에 다 할 수 없으므로 범위를 줄인 실행안을 둔다.

| 단계 | 내용 | 산출물 |
|---|---|---|
| 1 | 허용집합 $\mathcal{A}$ 계산 (덕트 포함 SDF) | 세 단면에서 $\mathcal{A}$ 시각화, 실행불가 진단 |
| 2 | $\mu_{\text{flex}}$ 를 파이버 해석에서 유도, $c_{na}$ 고정 가정 | 수요장 그림 |
| 3 | 고정 $\mu$ 에 대한 배치 (CVT 또는 OT + 간격 제약) | 배치 결과, 기준 충족 판정 |
| 4 | $c_{na}$ 결합 풀기 (P1 고정점 반복) | 수렴 실험, 정리 시도 |
| 5 | 표준 지름 양자화 (P3) | $M_n$ 비교표 |
| 6 | 실도면 대조 (P8) | 좌표 오차, $M_n$ 차이 |

1–3 이 서면 "장으로 배근한다"가 실증되고, 4 가 서면 이론적 중심이 생긴다.
5–6 이 서면 논문이 된다.

## A.7 위험

- **범위가 커진다.** 최적수송·CVT 는 배워야 할 이론이고, 구현도 현재 엔진보다 무겁다.
- **P1 이 안 풀릴 수 있다.** 고정점이 유일하지 않거나 반복이 발산할 수 있다.
  그 경우에도 "조건부 수렴"이나 반례 제시가 결과가 되지만, 계획대로는 아니다.
- **기존 구현 상당 부분을 다시 쓴다.** 다만 A.5 대로 기하 엔진과 사례는 살아남는다.
- **지도교수 합의가 필요하다.** 주제의 성격이 "APF 응용"에서 "구조·최적화·전산기하의
  경계 문제"로 바뀐다. 심사위원 구성에도 영향이 있다.
